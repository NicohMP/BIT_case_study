"""LLM-based signal extraction (skeleton).

This module will eventually:
- Classify a market as relevant to equities (score + event type + mechanism)
- Map the market to impacted tickers in the tracked universe
- Produce structured outputs for storage in Postgres
"""

