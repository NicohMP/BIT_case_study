"""LLM pipeline tasks (skeleton).

This module will eventually:
- Run market relevance classification and store `signals`
- Extract ticker impacts into `signal_impacts`
- Generate scheduled reports into `reports` / `report_items`
"""

