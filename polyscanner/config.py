"""Configuration management (skeleton).

This module will eventually define how config is loaded/validated:
- `.env` for local development
- environment variables for deployment
- defaults and type validation

Keep keys aligned with `.env.example`.
"""

