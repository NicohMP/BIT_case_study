"""App entrypoint (skeleton).

This file will eventually create the web application (UI + APIs) and wire up:
- Background scheduling (ingestion + classification + report generation)
- Configuration loading
- Logging/observability

Left intentionally empty to avoid locking in framework choices too early.
"""

